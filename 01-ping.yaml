#playbook-1
- name: ping the server
  hosts: web #which hosts your ansible wants to connect
  tasks: #list of tasks/modules/collections
  - name: ping the server
    ansible.builtin.debug:
      msg: "This is from play book 1 and task1"
